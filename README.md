criar uma classe consumidor com os atributos , nome, cpf, endereço
o atributo/campo endereço tem que ser uma outra estrutura / classe
	Tipo do logradouro, nomeDoLogradouro, numero, cidade

Na execução através do método main, quero o atributo endereço  Endereço endereco 

Estrutura de dados * 

Mostrar o endereço da aline no terminal ( console).

Foi utilizado o metodo TO String para retornar uma representação do texto (String).
